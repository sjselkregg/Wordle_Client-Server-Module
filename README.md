# Wordle_Client-Server-Module
A simple command line based wordle app written in python. This uses a client, server and library with functions that communicate with sockets.


To run, make sure you have a python compiler visible in your path where this application is stored.

Use two terminals.

In the first terminal run: python wordleServer.py

In the second terminal run: python wordleClient.py
